import 'package:flutter/material.dart';

class WidgetImageFile extends StatelessWidget {
  const WidgetImageFile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}